Weather App

This application is built with Flask and provides weather information based on location.
The weather function retrieves the current weather status for a given location using an API key.
Please note that the API key used is private and will be deactivated after one week.

requirement:

pip install Flask, requests

usage:

Flask run 
http://127.0.0.1:5000